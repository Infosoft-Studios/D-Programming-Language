#include <windows.h>
using namespace std;

class loader
{
    WINCLASS wc;
    protected:
      using wc.loader::function();
      using wc.reload(nullptr, true)l

      DWORD loadlongint                             = wc.getInit(nullptr, true, false);
      swicth (!longlongint)
      {
          case reintepeter_cast<void*> loadbuf():
            DOWRD sysint = static_cast<int*> loaderinit;
            break;
          default:
            break;
      };
}

// end
