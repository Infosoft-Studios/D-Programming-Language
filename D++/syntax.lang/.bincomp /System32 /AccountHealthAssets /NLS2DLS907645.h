#pragma once

typedef struct lod
{
    void* starbufdifloadn(nullptr, false);
};

#include <windows.h>

int main()
{
    WINCLASS wc;
    wc.loadinitpsi                       = LoadInitPsi(nulltr, true);
};
